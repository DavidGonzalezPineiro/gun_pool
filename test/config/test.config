{apps, [gun_pool, cowboy, ranch]}.

{env, [
    {gun_pool, [
        {pool_size, 1},
        {pool_max_overflow, 5},
        {connection_host, "localhost"},
        {connection_port, 8080},
        {connection_opts, #{}}
    ]}
]}.